# 디자인 시스템 — 퀀트플랫폼 (MyStock / quantman)

웹앱 UI는 **이 문서를 단일 기준(SSOT)** 으로 한다. 새 화면·컴포넌트는 여기 정의된
토큰(색·폰트·간격·컴포넌트·차트 규칙)만 쓰고, 벗어나야 할 땐 **먼저 합의**한다.

> **코드상의 진실원본:** 웹 토큰의 실제 값은 `web/src/index.css`의 `:root`에 있다.
> 이 문서는 그 값의 **의미·사용 규칙**을 설명한다. 토큰을 바꿀 땐 `:root`와 이 문서를
> **함께** 고친다(둘 중 하나만 고치면 가이드가 다시 낡는다 — 과거 실패 사례).
>
> **공동작업자(조대표 포함) 필독:** UI 작업 전 이 문서를 읽고, 임의의 색/폰트/간격을
> 새로 만들지 않는다. 회색 기본 위젯·임의 hex·새 폰트 도입은 금지.

---

## 1. 제품 맥락

- **무엇:** 한국 주식 퀀트/자동매매 SaaS. 웹앱에서 종목·산업을 분석하고 매매 전략을
  수립하면, 사용자 PC의 로컬앱이 그 전략으로 모의투자를 자동 실행한다.
- **대상:** 퀀트 트레이딩을 시작하려는 초·중급 개인투자자.
- **표면:** 웹앱(React SPA — 분석·전략) + 로컬앱(Python Tkinter — 자동 실행).
- **이 문서의 범위:** 주로 **웹앱**. 로컬앱(`local/localapp/gui.py`)은 별도 팔레트로,
  웹 토큰 변경 시 동기화가 필요하면 그때 gui.py를 확인해 맞춘다(웹값을 gui.py에 그대로
  가정하지 말 것).

## 2. 디자인 원칙

1. **흐름을 보이게.** 분석 → 전략 → 모의투자 → 기기 연결 → 자동매매. 사용자는 늘
   "지금 어디에 있고 다음에 뭘 할지" 알 수 있어야 한다.
2. **숫자를 해석해 준다.** 지표를 그대로 던지지 않는다. 등급·평이한 문장·툴팁으로
   초중급 사용자가 판단할 수 있게 한다. (제품의 핵심 차별점)
3. **신뢰감.** 돈을 다루는 도구다. 회색 기본 위젯이 아니라 **일관된 네이비 톤**,
   명확한 상태 표시, 빈 상태 안내로 신뢰를 준다.
4. **절제.** 타이포·여백·테두리가 일한다. 그라데이션·과한 장식·등장 애니메이션 없음.

## 3. 아이덴티티

- **방향:** 금융 도메인의 **정직한 네이비**. 딥 네이비 크롬(상단바/사이드바) + 다크
  차콜 본문 + **골드 액센트**. AI 클리셰(검정+형광)가 아니라, 증권·금융 화면이 주는
  전문성·신뢰의 색.
- **손익 색은 한국 관례:** **상승·수익=빨강 / 하락·손실=파랑** — 연동 브로커(KIS)·국내
  증권사 화면과 일치. (미국식 green/red 아님)
- **맥락 분리:** 골드는 **UI 크롬·강조·차트 선**에만, 수익 빨강은 **방향성 숫자**에만
  써서 색이 겉돌지 않게 한다.

---

## 4. 색상 토큰

실제 값은 `web/src/index.css` `:root`. 아래는 역할·용도.

### 네이비 (크롬·강조)
| 토큰 | HEX | 용도 |
|------|-----|------|
| `--navy-950` | `#0a1a30` | 상단바·사이드바 딥 네이비(가장 어두움) |
| `--navy-900` | `#0f2342` | 헤더·강조 영역 |
| `--navy-700` | `#1d3a63` | 보더·활성 강조 |
| `--navy-300` | `#93a4c0` | 네이비 배경 위 뮤트 텍스트 |

### 표면 (다크 차콜)
| 토큰 | HEX | 용도 |
|------|-----|------|
| `--bg` | `#13161c` | 페이지 배경(차콜) |
| `--panel` | `#1c212b` | 카드·패널(약간 밝은 차콜) |
| `--border` | `#2b323e` | 테두리·구분선 |
| `--input-border` | `#3a4250` | 입력 테두리(카드 위 대비 확보) |
| `--text` | `#d2d8e0` | 본문 글자(라이트 그레이) |
| `--muted` | `#8b94a3` | 보조 텍스트(슬레이트) |

### 액센트 (골드)
| 토큰 | HEX / 값 | 용도 |
|------|-----|------|
| `--accent` | `#d4a738` | 골드 — 포커스 링·탭 밑줄·게이지·차트 강조 등 **큰·장식** |
| `--accent-strong` | `#e0b958` | accent의 **텍스트용** 밝은 변형 — 링크·활성 nav·배지·소형 강조 |
| `--accent-soft` | `rgba(196,152,43,0.16)` | 골드 배경 틴트(hover·선택 행) |
| `--accent-hover` | `#c4982b` | 버튼 hover |
| `--ink` / `--ink-hover` | `#2f5390` / `#3a629f` | **주요 버튼 채움**(다크 위 보이는 네이비) |

### 시장 방향 (한국식 — 방향성 숫자 전용)
| 토큰 | HEX / 값 | 용도 |
|------|-----|------|
| `--up` / `--up-soft` | `#de3033` / `rgba(222,48,51,0.18)` | 상승·수익·매수 |
| `--down` / `--down-soft` | `#1668c4` / `rgba(22,104,196,0.22)` | 하락·손실·매도 |

### 상태/품질/위험 (시장 방향과 별개)
| 토큰 | HEX / 값 | 용도 |
|------|-----|------|
| `--green` / `--green-soft` | `#2ea65a` / `rgba(46,166,90,0.16)` | 정상·완료·연결됨 |
| `--red`(=`--danger`) / `--red-soft` | `#e0504f` / `rgba(224,80,79,0.16)` | 오류·위험·실전 경고 |
| `--amber` / `--amber-soft` | `#d08a2c` / `rgba(208,138,44,0.16)` | 주의·설정 미완료 |

### 색 사용 규칙
- **방향성 숫자**(수익률·손익·가격변화·매수/매도) = `--up`(빨강)/`--down`(파랑). `.pos`/`.neg`.
- **상태/품질** = green(정상·완료)/red(오류·위험)/amber(주의).
- **주요 액션** = `--ink` 채움, **브랜드 강조·활성·링크** = `--accent`/`--accent-strong`.
- 골드를 **채움**으로 쓰고 그 위에 글자를 올릴 땐 글자를 **어두운색**으로(예: 가입 버튼
  `#1a1206`). 골드를 **텍스트**로 쓸 땐 `--accent-strong`(다크 위 가독).
- ⛔ 임의 hex·회색 기본 위젯 금지. 위 토큰으로만.

---

## 5. 타이포그래피

- **스택:** `Arial, "맑은 고딕", "Malgun Gothic", -apple-system, sans-serif`.
  Arial을 앞에 둬 **영문·숫자는 Arial, 한글은 맑은 고딕**으로 렌더된다(사용자 지정).
- **숫자:** `:root`에 `font-variant-numeric: tabular-nums` — 표·KPI·수익률 숫자가
  고정폭으로 세로 정렬. 트레이딩 화면 신뢰감의 핵심.
- **모듈 표준 크기(필수):** HOME·산업분석 등 모듈(`.mod-std`·`.industry-67`)은
  **대제목(h1) 22pt · 소제목(h3) 16pt · 본문/표(table·데이터) 12pt** 로 통일.
  - index.css에서 `!important`로 `.panel h3(15px)` 등 기본을 덮는다.
  - **글자크기/폰트 수정 요청 시 이 세 단계를 항상 함께** 본다(대/소/본문).
  - "모든 데이터 본문은 12pt 고정" — 표·뉴스·컨센서스·리스트 등 데이터 텍스트 공통.
- **모듈 밖 기본값(참고):** `h1` 1.4rem · `h3` 0.98rem · `.panel h3` 15px ·
  보조 12px · 캡션 11px.

---

## 6. 간격·레이아웃

- 패널 `padding: 20px`, 패널 간 `margin-bottom: 18px`.
- 모서리 반경 `--radius: 10px`(칩·버튼 7~8px, 알약형 pill 999px).
- 본문 컨테이너 `.main-inner` `padding: 28px 32px`, `max-width: 1100px`
  (개별종목분석은 `.dashboard-fullwidth`로 1100px 캡 해제).
- **밀도:** HOME·산업분석은 `zoom: 0.67`(`.mod-std`·`.industry-67`) — 브라우저 67%
  줌과 같은 밀도를 100%에서 기본 제공.
- 상단 네비게이션은 **2행**: 1행 = 로고(좌·고정) + 로그인/회원가입(또는 계정, 우),
  2행 = 네비 링크. `.topnav` 계열, sticky top, `--navy-950` 배경.

---

## 7. 컴포넌트 패턴

- **패널(`.panel`):** 카드(`--panel` 배경 + 1px `--border`). 모든 콘텐츠 묶음의 기본 단위.
- **지표 카드(`.stat`/`.stat-box`):** 라벨(`--muted` 12px) + 큰 값(22px). 값에 `.pos`/`.neg`.
- **탭(`.tabs`/`.tab`):** 하단 밑줄형. active = `--accent-strong` 텍스트 + 골드 밑줄.
- **세그먼트 토글(`.seg`/`.mode-filter`):** 한 묶음 안에서 택1. on = `--panel` 칩 + 그림자.
- **칩(`.chip`):** 골드 틴트 배경 토큰. 빈 칩(`.ghost-chip`)은 점선·투명으로 "비었음" 구분.
- **배지(`.badge`):** pill. 기본 골드 틴트, `.gray`는 muted.
- **버튼:**
  - 기본 = `--ink` 채움(네이비), 흰 글자, `padding: 9px 16px`, `radius: 8px`, weight 600.
    hover `--accent-hover`, active `--accent-strong`, disabled `--border`/`--muted`.
  - 보조 = `.ghost`(bg + border). 작은 건 `.sm`(5px 10px).
  - **인증 CTA:** `.auth-cta.login`(투명 + 흰 보더), `.auth-cta.signup`(골드 채움 + 어두운 글자).
  - 강조 액션 = `.apply-btn`(골드 채움). 위험/삭제 = `.x-btn`(hover 시 `--red`).
- **드롭다운(`.account-menu`):** `--panel` + 그림자, 항목 hover = `--accent-soft`.
- **테이블:** `border-collapse`, 숫자 우측정렬, **첫 열 좌측정렬**, `thead`는 `--muted`.
  열 폭이 펼침에 따라 흔들리면 안 됨 → 다열 표는 `table-layout: fixed` + `colgroup`.
- **정보 아이콘(`.info-tip`):** 라벨 옆 `?`, hover 시 한 줄 설명. 전문용어
  (MDD·샤프·CAGR·PER·EV/EBITDA 등)에는 붙인다.
- **빈 상태:** 회색 한 줄 금지 — 제목 + 안내 + CTA.

### 스크롤바
모듈(`.mod-std`) 내부 스크롤바는 **매우 얇은 골드**.
- WebKit(Chrome/Edge): `::-webkit-scrollbar { width/height: 2px }` + thumb
  `rgba(196,152,43,0.55)`.
- ⚠ **표준 `scrollbar-width/color`를 `.mod-std`에 직접 주지 말 것** — 최신 Chrome이
  `::-webkit-scrollbar` 폭을 무시하고 두꺼운 'thin'을 그린다. 표준 속성은
  `@supports (-moz-appearance:none)`로 **Firefox에만** 적용한다.

---

## 8. 차트 규칙 (recharts) — **이후 모든 차트 공통**

색·표기를 통일해 어느 모듈에서든 같은 차트 언어를 쓴다.

- **색은 네이비 막대 + 골드 선만. 회색 등 기타 색 금지.**
  - `CHART_NAVY = "#264a85"`(막대), `CHART_GOLD = "#d4a738"`(선).
- **막대 = 절대 금액. 막대 높이/방향이 항상 값의 방향과 일치**(증감을 막대로 표현).
- **선은 "수준 지표"(마진%·ROE%·부채비율% 등)에만 쓴다.** YoY/QoQ 같은 증감률은 절대값과
  **방향이 반대로 움직일 수 있어**(값↓인데 감소율 완화로 선↑) 막대 위에 선으로 겹치면
  "증가처럼" 오해를 준다 → **증감률을 선으로 그리지 않는다.** 증감률은 **막대 hover 툴팁 +
  표의 증감률(YoY/QoQ) 행**으로 제공(정보는 보존).
- **이익률(마진) 항목**(매출총이익·영업이익·당기순이익·EBITDA): 막대=금액 + 우축에 **마진% 선**.
- **% 만 있는 지표**(ROE·부채비율·차입금비율): 막대 대신 **선그래프**(수준 추세).
- **x축 라벨:** 연간 = `FY23`(FY+2자리), 분기 = `1Q26`(분기Q+2자리). 03→1Q·06→2Q·
  09→3Q·12→4Q.
- **막대 위 값 라벨**: 소수점 1자리.
- **단위는 차트별로 일관:** |값| 최대 ≥ 1조(=10,000억)면 **조**, 미만이면 **억**.
  축·라벨·툴팁·헤더 단위를 모두 같게.
- **레이아웃:** 한 행을 그 탭의 **차트 개수만큼** 균등 분할해 폭을 꽉 채운다
  (`repeat(N, minmax(0,1fr))`).
- **모션:** `isAnimationActive={false}`(등장 애니메이션 없음 — §9 모션 원칙).

## 9. 모션

미니멀-기능적. 색·투명도 전환만(~120ms). 등장 애니메이션·스크롤 연출 없음.
(예외: 산업분석 트리맵 zoomable 전환 `.tm-zoom` — 위치/크기/투명도 보간.)

---

## 10. 자동매매 정책 (Phase 9A~)

베타 자동매매는 다음 best practice를 글로벌 default로 적용한다. 전략별 override는
`Strategy.execution` 필드로 가능.

1. **시장가 → 지정가 + price tolerance.** 신규 진입 어제 종가 ×(1+1%) 지정가, 청산 −2%.
   미체결 시 5분 후 자동 취소 → 그날 신호 폐기. 호가 단위 자동 라운딩.
2. **갭 필터.** 전일 종가 대비 갭 ±2.5% 초과 시 신규 진입 폐기. 매도 시그널은 손절
   위해 갭 필터 무시(단 limit tolerance로 보호).
3. **일일 손실 한도 + kill switch.** 자본 대비 −3% 도달 시 자동 전량 청산 + 신규 차단.
   사용자 명시적 reset 전까지 유지.
4. **ATR 변동성 보정 사이징.** 수량 = (자본 × 1% risk) ÷ (ATR × 2). 단일 종목 비중
   자본의 10%로 클램프. 레거시 `pct_cash` 모드도 지원.
5. **백테스트 슬리피지·갭 가정.** 편도 25bps 수수료 + 10bps 슬리피지 default, 갭 1%
   초과분 절반을 추가 비용 산입.

---

## 변경 이력

| 날짜 | 결정 | 근거 |
|------|------|------|
| 2026-05-20 | 초기 디자인 시스템 문서화 | 토큰·패턴 명문화 |
| 2026-05-20 | Phase 9A — 자동매매 5 best practice | ExecutionPolicy 스키마 + 글로벌 default + override |
| 2026-05-22 | 브랜드 색 → 테라코타 + 크림 | 당시 사용자 요청(이후 네이비로 전환됨) |
| 2026-05-23 | 접근성·대비 패스 | accent-strong/input-border 신설, 컨트롤 통일 |
| (이후) | **웹앱 네이비 금융 팔레트로 전환** | 코드(`index.css`)는 네이비+골드+다크 차콜로 바뀌었으나 이 문서가 미갱신 → 가이드-코드 불일치로 통일성 붕괴 |
| 2026-06-18 | **문서 전면 갱신 — 코드와 재동기화 + 차트 규칙 명문화** | DESIGN.md가 구(舊) 테라코타/크림/Pretendard로 남아 있어 디자인 불일치의 근본 원인이 됨. 현행 `index.css` :root(네이비 `#0a1a30~`, 골드 `#d4a738`, 다크 차콜 `#13161c`, Arial/맑은고딕, mod-std 22/16/12pt·zoom 0.67, 얇은 골드 스크롤바)를 반영해 색·타이포·간격·컴포넌트·버튼을 재작성. **§8 차트 규칙 신설**(네이비 막대+골드 선, 마진/%-선, FY·nQ 라벨, 조/억 단위 일관, 행 꽉채움). SSOT 원칙 명시(토큰 변경 시 :root와 본 문서 동시 수정). |
