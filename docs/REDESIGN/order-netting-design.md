# 발주창 넷팅 (Cross-Strategy Order Netting) 설계

**작성:** 조대표 · 2026-07-02
**모듈:** 자동매매 엔진 (`local/localapp/`, 서버 `routers/trading.py`, 웹 트레이딩 탭)
**상태:** 설계 승인 → 구현 계획 대기

---

## 1. 문제 (일반)

**여러 전략을 함께 연동하면, 같은 종목·같은 발주창에서 서로 반대 방향의 주문이 겹칠 수 있다.**
현재는 각 전략의 주문을 독립적으로 즉시 발주하므로, 겹치는 수량만큼 **불필요한 왕복 수수료·
슬리피지**가 발생한다. 실물 순포지션은 그대로인데 "팔고 즉시 되사는(또는 그 반대)" 거래가 나간다.

이는 특정 전략 조합의 문제가 아니라 **구조적 문제**다. 반대 주문이 겹치는 경로는 여럿이다:
- 청산(매도) vs 진입(매수) — 롱 청산과 롱 진입이 겹침
- 진입(매수) vs 진입(매도) — 한 전략 롱 진입, 다른 전략 숏 진입(선물)
- 청산(매도) vs 청산(매수) — 한 전략 롱 청산, 다른 전략 숏 청산
- 선물뿐 아니라 **주식**도(롱 청산 매도 + 롱 진입 매수 = 워시)

**진실원천:** [runner.py `run_close_cycle`](../../local/localapp/runner.py) 이 종가창에서
`liquidate_day_trades`(청산) → `enter_close_candidates`(진입)를 **각각 즉시 발주**하고,
아침 cycle(`_cycle_body` §2 청산 → §3 진입)도 동일하다. 둘 사이·전략 간 상쇄가 전혀 없다.

### 대표 예시 (A/B — 이 요구의 발단, 일반 규칙의 한 사례일 뿐)

- **전략 A** (S&P500 역추종 당일매매): 하락일 → 시가매수·종가매도(롱), 상승일 → 시가매도·
  종가환매(숏). `direction=long_short`, `fill=next_open`, `hold_days=0`.
- **전략 B** (오버나이트 롱): 항상 종가매수 → 익일 시가매도. `fill=close`.

**하락일**엔 두 발주창 모두에서 반대 주문이 겹친다:

| 발주창 | 청산(전략) | 진입(전략) | 결과 |
|---|---|---|---|
| 종가 (15:40) | A 롱 **매도** | B 롱 **매수** | 왕복 낭비 |
| 익일 시가 (08:55) | B 롱 **매도** | A 롱 **매수** | 왕복 낭비 |

상승일엔 동방향(둘 다 매수 또는 매도)이라 순포지션이 실제로 전환(−1↔+1)되므로 두 주문이 정당하다.

## 2. 목표 · 비목표

**목표:** 한 발주창에서 같은 종목의 **반대 방향 주문이 겹치는 수량만큼** 브로커 주문을
내지 않고 원장만 이관해, 왕복 수수료를 제거한다. 하락일 종가·시가 양쪽 모두.

**비목표:**
- 백테스트 엔진의 종가매수→익일시가매도 패리티(별건 — 라이브만 대상).
- 상계증거금 자체의 재설계(넷팅은 *주문*을 줄일 뿐, 증거금 계산은 브로커 소관).
- 동방향 주문 병합(수수료는 계약당이라 병합해도 절감 없음 — 하지 않는다).

## 3. 핵심 규칙 — 종목별 순증분(net signed delta)

> ⚠️ **최종 메커니즘은 §13(근본 아키텍처 v2)이 정본이다.** 아래 §3~§5.4는 개념적 기반이며,
> 감사(§12) 반영 후 §13에서 "(contract_code, 포지션 side)별 합성-체결 핸드오프"로 정련됐다.
> 구현은 §13을 따른다.

전략·청산/진입 구분과 **무관하게**, 한 발주창에서 종목 S에 대한 **모든 발주 의도**를
부호 합산한다(매수 +, 매도 −):

```
net_S = Σ(매수 수량) − Σ(매도 수량)
```

- **브로커로 나가는 실주문 = net_S** (양수면 |net_S| 매수, 음수면 |net_S| 매도, 0이면 무발주).
- **겹쳐 상쇄되는 수량 = `min(Σ매수, Σ매도)`** — 이 수량은 브로커 주문 없이 원장만 이관(§4).

이 한 규칙이 모든 반대 주문 조합(청산-진입·진입-진입·청산-청산)을 자동 처리한다. 상승일처럼
모든 주문이 동방향이면 `min(Σ매수,Σ매도)=0` → 상쇄 없음 → 전량 정상 발주(순포지션 실제 전환).

**절약 수수료** = `min(Σ매수, Σ매도) × 계약당수수료 × 2` (겹친 계약마다 매수·매도 한 쌍씩 회피).

**불변식 — 상쇄 대상이 없으면(min=0) 입력 주문을 그대로 발주한다(no-op).** 이것이 현행 동작
byte-identical 보존을 보장한다(특히 골든 테스트가 있는 아침 cycle). 종목이 하나뿐인 전략이든
여러 전략이 한 종목에 겹치든 동일 규칙 — 겹침이 없으면 아무것도 바뀌지 않는다.

## 4. 장부 이관 (book-transfer) — 전략별 분리 손익

상쇄되는 `min(Σ매수,Σ매도)` 계약은 **브로커 주문 없이** 원장에서만 이동한다. 실물 포지션은 불변.
각 발주 의도(leg)를 **기준가**로 장부 처리하되, 손익은 leg마다 독립적으로 산정한다:

- **청산 leg** (상쇄분): `realized = (기준가 − 진입가) × 수량 × 부호 × 승수`를 해당 전략 명의로
  `trades.jsonl`에 기록, 원장 슬롯 감소(0이면 삭제) — **`netted=true`·수수료 0**.
- **진입 leg** (상쇄분): 해당 전략 명의로 원장 슬롯 생성/증가(진입가=기준가), 진입 기록 —
  **`netted=true`·수수료 0**.
- **잔여**(net_S 만큼)는 진입/청산 의도 중 일부가 실제 브로커로 발주돼 **실제 체결가**로 기록되고,
  겹친 나머지는 기준가로 장부 처리된다. 어느 leg이 실주문(체결가)·어느 leg이 장부(기준가)를
  받는지 배분은 **FIFO로 결정론적 고정**(구현 계획). 이 배분은 잔여 수량의 `(체결가−기준가)`
  차이만큼만 특정 전략 원가에 영향 — 같은 발주창(≈동시 체결)이라 미미하나, 결정론적이라 재현 가능.

**전략·leg 수 무관:** 청산 2건 + 진입 3건이 겹쳐도 각 leg의 손익은 기준가 기준으로 독립 산정되므로
**closer↔opener를 특정 쌍으로 매칭할 필요가 없다**(matching-free). A/B는 이 규칙의 1:1 사례.

**기준가:** 넷팅 시점의 현재가(`_safe_price`, 종가/시가 무렵) — 실제 청산/진입이 썼을 참조가와 동일.

**손익 정합성(정직성):** 예로 하락일 A롱청산+B롱진입 →
A레그 `(기준가−A진입가)` + B레그 `(익일시가−기준가)` = `(익일시가−A진입가)` = 실제 총손익.
**분할된 레그들의 합이 정확히 참값**이므로 분리해도 정직하다. 기준가는 유일한 분할점이며
`netted` 표시로 "실제 체결 아님·기준가 분할"을 명시한다.

## 5. 통합 구조 — "결정 → 넷팅 → 발주" seam

현행은 결정 즉시 발주한다. 결정(주문 의도 산출)과 발주 사이에 넷팅을 끼운다.

```
local/localapp/netting.py            [신규 · 순수 함수 · 브로커/IO 없음]
    net_window(intended_orders) -> NetResult(
        broker_orders,   # 실제 발주할 순증분 주문 (종목별 net_S)
        book_legs,       # 브로커 없이 장부 처리할 leg (netted 청산/진입)
        savings)         # [{symbol, netted_qty, commission_saved_krw}]
    · intended_orders = 청산·진입 구분 없이 이 발주창의 모든 발주 의도 한 리스트
      [{sid, symbol, strategy, side(buy/sell), qty, ref_price, kind(entry/exit),
        entry_price, definition, ...}]
    · 종목별 부호합산 → net_S 만 broker_orders, min(Σ매수,Σ매도)은 book_legs, 절약 계산
    · 겹침 없으면(min=0) 모든 의도를 broker_orders로 그대로 반환 (no-op = 현행 보존)

trader.py
    · liquidate/enter 경로를 '주문 의도 산출' + '발주'로 분리(청산 클램프·게이트는 산출 단계)
    · _apply_book_leg(leg): 원장 이관 + netted 손익/진입 기록 (브로커 미호출)
    · broker_orders 는 기존 _submit_* 로 정상 발주

runner.py
    · run_close_cycle: (청산 의도) + (진입 의도) 합쳐 net_window → book_legs 적용 + broker_orders 발주
    · 아침 cycle 경로: 동일 seam 적용 (no-op 보장으로 무넷팅 시 현행 보존)

order_log.py
    · 주문/거래 레코드에 netted(bool)·commission_saved 필드
    · cycle 요약에 n_netted·commission_saved_krw

gui.py / gui_format.py
    · 주문내역에 넷팅 행 구분 표시 + 절약액

server/app/routers/trading.py (+ models)
    · cycle 요약의 n_netted·commission_saved_krw 수용·저장 (집계만 — 보안 §4)

web (트레이딩 탭)
    · "오늘 넷팅 N건 · 수수료 X원 절약" + 사이클별 상세
```

## 5.5 진입 사이징 기준 — 넷팅의 핵심 정합성

**문제:** 현재 진입 사이징은 발주마다 브로커 `orderable_qty`를 **live로 재조회**하고
([trader.py:1243](../../local/localapp/trader.py#L1243)), 잔고도 매 호출 재조회한다
([trader.py:1100](../../local/localapp/trader.py#L1100)). 즉 **청산이 먼저 체결되어 마진이 회수된
계좌상태**를 진입 사이징이 본다. 넷팅은 청산을 체결시키지 않으므로, 순진하게 하면 진입이 회수될
마진을 못 보고 **0계약으로 사이징 → 전략 붕괴**한다(하락일 오버나이트=0).

**해결 — 넷팅될 청산이 회수할 여력을 진입 사이징에 반영한다:**

```
유효여력(S) = live_orderable(S) + Σ(같은 종목 넷팅 대상 청산 계약수)
```

같은 종목 = 같은 증거금률이므로 "N계약 청산 = N계약분 여력 회수"가 **정수로 정확히 성립**한다
(주식도 동일: N주 청산 = N주분 현금 회수, 같은 가격). 로컬 마진 수식 계산 없이 정수 덧셈만 —
모델 A의 "브로커 실값 사용" 정신을 유지한다.

**단, 넷팅되는 종목에 한해** 사이징 자금원을 "live 재조회"에서 "스냅샷 + 로컬 여력 순차 차감"으로
바꾼다(다중 진입전략이 같은 종목을 노릴 때 회수 마진 중복 계산 방지 — E2). 넷팅 없는 종목은
현행 live 재조회 그대로(현행 보존).

**한계(정직):** 브로커 마진이 비선형(상계·유지증거금 계층)이면 유효여력은 근사다. 근사가 실제보다
크면 브로커 거부가 2차 안전망(과다발주 차단), 작으면 진입이 약간 보수적. 이 근사의 실측 검증은
모의계좌 라이브 단계에서 확인한다(설계 단계 검증 불가 항목으로 명시).

## 6. 보존할 불변식 (구현 시 필수 확인)

- **무넷팅 = 현행 byte-identical** (아침 cycle 골든). `net_window` no-op 경로로 보장.
- **청산 먼저 → 진입 나중.** 잔여 발주도 청산 먼저.
- **KIS 실보유 클램프(L-04).** 넷팅은 *클램프된* 청산 수량 기준으로 상쇄.
- **킬스위치·drawdown 게이트.** 게이트로 막힌 진입은 넷팅 대상 아님 → 청산은 정상 발주.
  (넷팅은 게이트 통과한 진입 의도만 대상으로 한다.)
- **유령 진입 불가.** 이관은 기존 원장 포지션만 이동.
- **보안(§4).** 서버엔 집계(건수·절약 KRW)만. 계좌번호·원시주문 미전송.

## 7. 수수료 절약액 산정

절약 = 상쇄된 수량의 **왕복** 수수료 = `m × 계약당수수료 × 2(청산+진입)`.
계약당 수수료는 **엔진 사이징이 쓰는 동일 수수료율**에서 가져온다(별도 하드코딩 금지 —
구현 계획에서 실제 출처를 고정). 슬리피지/스프레드 절약은 추정이 부정확하므로 집계에서 제외하고
문구로만 "실제 절감은 이보다 큼(스프레드 포함)" 정도로 안내.

## 8. 테스트 전략

| 단위 | 방식 | 성공 기준 |
|---|---|---|
| `netting.py` | 순수 단위 | net_S 정확·다종목 독립·**모든 반대 조합**(청산-진입·진입-진입·청산-청산)·수량불일치 잔여·동방향 무상쇄·**겹침없음 no-op(입력=출력)**·주식/선물 공통 |
| `trader._apply_book_leg` | MockBroker 시나리오 | 이관 후 원장 정확·realized 정확·**브로커 주문 0**·N-leg(청산2+진입3) 합계 정합 |
| `run_close_cycle`/아침 cycle | MockBroker 시나리오 | 하락일 넷팅 발생·순증분만 발주·**무넷팅 시 현행 결과 동일(골든)** |
| order_log/gui_format | 단위 | netted 표시·절약 집계 |
| server trading | 서버 테스트 | n_netted·commission_saved 수용·저장 |

## 9. 담당 · 협업

- local·server 자동매매 계층 = 조대표(본 설계자).
- **웹 트레이딩 탭 표시**는 착수 전 담당 확인 — 희제 영역이면 협의(계획 단계). 웹은
  로컬·서버 완료 후 후속으로 분리 가능(로컬앱 표시가 1차 투명성).
- 다른 세션 `feat/dev-local-visual-testenv`(scripts·web dev 라우트)와 파일 비겹침(저위험).

## 10. 단계 (구현 계획에서 상세화)

1. `netting.py` 순수 함수(종목별 net_S) + 단위 테스트 — 모든 반대 조합·no-op (TDD).
2. `trader.py` 결정/발주 분리 + `_apply_book_leg` + netted 로깅.
3. `runner.py` 종가 cycle 배선 + 시나리오 테스트.
4. 아침 cycle 배선 + 골든 보존 확인.
5. order_log/gui/gui_format 투명성 표시.
6. server 집계 수용 + (웹 트레이딩 탭 — 담당 확인 후).

## 11. 엣지 케이스 (구현·테스트에서 각각 커버)

| # | 케이스 | 위험 | 대응 |
|---|---|---|---|
| **E1** | 진입 사이징이 청산 체결 후 계좌상태에 의존 | 넷팅 시 진입 0계약 → 전략 붕괴 | §5.5 유효여력=orderable+상쇄청산수량. **핵심** |
| **E2** | 다중 진입전략이 같은 종목 | 회수 마진 중복 계산 → 과다사이징 | 종목별 여력 로컬 순차 차감(단일진입=자명) |
| **E3** | 잔여 실주문 부분체결/거부 | 장부이관 후 실주문 실패 시 원장≠실물 | 장부이관=기존 물량 라벨만 이동(무거래·항상 정합). 실물변경은 net_S 실주문뿐 → 기존 pending/reconcile 처리 |
| **E4** | KIS 실보유 < 원장 (외부 수동매도) | 유령 수량 상쇄 | 넷팅은 **클램프된** 청산수량만 대상(L-04). 초과분은 기존 orphan 처리 |
| **E5** | 킬스위치·drawdown이 진입 차단 | 청산만 남는데 넷팅 시도 | 게이트 통과한 진입만 넷팅 대상 → 막히면 청산 정상 발주 |
| **E6** | 같은 기초자산·다른 만기물(선물 롤) | 근월/원월 상계 오인 | 넷팅 키 = **정확한 종목코드**(만기물 다르면 다른 종목 → 상쇄 안 함) |
| **E7** | 사이클 재실행(스케줄러 재시도) | 이중 넷팅·중복 진입 | 장부이관이 원장 반영 → held-check가 재진입 skip(멱등성 확인) |
| **E8** | 크래시(장부이관 후 실주문 전) | 원장 부분반영 | 이관은 단일 락 + 원자적 저장, 실물 불변 → 재기동 reconcile 정합 |
| **E9** | catch-up(놓친 아침 복구) | 넷팅+지정가 복구 상호작용 | **catch-up은 넷팅 제외**(드문 복구경로·기준가 모호) — 정상 발주 |
| **E10** | 결정론(잔여 배분·전략 순서) | 어느 전략이 실체결가/기준가 받나 비결정 | strategy_id 정렬 FIFO로 결정론 고정 |
| **E11** | commingled 계좌(기존/수동 보유 + 전략이 같은 종목·같은 side) 부분 수동매도 | 매도 귀속 모호 → 원장 수량 오귀속 | 넷팅 외재(§12.5). 자금안전 무영향·min 클램프·EOD reconcile 재수렴 |

## 12. 감사 결과 (코드리뷰 검증 + 적대적 감사, 자금 테스트 없음)

두 독립 서브에이전트가 실제 코드(파일:라인)로 감사. **자금 테스트 없이 코드리뷰만.**

### 12.1 E1~E10 검증 (8 REAL · 1 PARTIAL · 1 반전)
- **REAL 8:** E1(사이징이 청산 체결후 계좌상태 의존), E2, E3(원장은 fill-driven), E4(클램프), E5(게이트 비대칭), E7(멱등성 다층 존재), E8(락+원자저장), E9(catch-up은 close cycle 미실행).
- **PARTIAL:** E10 — 로컬 순서는 결정론적(by_strategy 리스트·ledger 삽입순), 서버측 순서 생성은 미검증.
- **반전(NOT-REAL이나 더 위험):** **E6** — 원장·데이터셋·preview의 `symbol`은 **연속물 상품명**("코스피200선물"), 실제 만기 계약코드는 주문 시점에만 해석돼 `contract_code`로 별도 저장. → 상품명 키로 넷팅하면 **모든 만기물이 뭉쳐** 롤 경계에서 근월↔원월 오상계. **넷팅은 `contract_code`(물리 계약)로 해야 함.**

### 12.2 신규 케이스 (적대적 감사)
| # | 케이스 | 심각도 |
|---|---|---|
| **N1** | book-transfer가 intents.jsonl 멱등성 저널 우회 → 재시도/catch-up/크래시 시 **중복 실주문** | **BLOCKER** |
| **N2** | 무체결 book leg이 `record_slippage`에 fill로 기록돼 슬리피지 통계 오염 | HIGH |
| **N3** | 넷팅 leg이 서버 타임라인 카운트에 안 잡혀 false-green / 또는 거래수 부풀림 | HIGH |
| **N4** | `enrich_positions`가 종목당 1전략 가정 → 이관 시 전략/peak/진입가 오표시·트레일링 워터마크 리셋 | HIGH |
| **N5** | 잔여 부분체결 시 reconcile이 원장≠브로커를 "외부 수동매도"로 오판·**원장 자동삭제** | HIGH |
| **N6** | WS 체결 스레드 `reload_state()`가 다중-leg 이관 중간에 끼어들어 원장 clobber | HIGH |
| **N7** | 유효여력이 넷팅될 청산의 회수마진을 크레딧 → 청산이 실제 클램프/실패면 잔여 실주문 과대 | HIGH |
| **N8** | book-leg 실현손익이 통화(FX)/승수를 `_apply_fill`과 다르게 처리 → USD 종목 P&L 오류 | MEDIUM |
| **N9** | §5.5 사이징 자금원 분기가 같은 창의 비넷팅 종목 사이징까지 흔들어 no-op 깨짐 | MEDIUM |
| **N10** | 주문-side 넷팅이 원장 포지션-side(롱/숏) 무시 → 롱청산 vs 숏청산 오상계 | MEDIUM |
| **N11** | book leg이 체결후 킬스위치 훅 미실행(대체로 무해하나 락 재진입 deadlock 인접) | LOW |

### 12.3 두 구조적 뿌리 (단건 패치 금지 근거)
- **(가) 무체결 원장변경의 병렬 우주:** book leg이 intent 저널·pending·슬리피지·reconcile을 우회. 이 기계들은 전부 *"모든 원장변경 = 브로커 체결"*을 전제 → 그 전제를 깨는 순간 N1·N2·N5가 동시 발생.
- **(나) side 불일치:** 넷팅=주문 side, 원장/`_apply_fill`=포지션 side·sid 슬롯. 한 종목 다전략에서 N4·N10.

→ **§13 근본 아키텍처가 이 두 뿌리를 구조로 닫는다.**

### 12.4 구현 후 최종 독립 검토 (코드리뷰, 자금 테스트 없음)

구현 완료 후 독립 서브에이전트가 실제 구현 코드를 감사 findings에 대조:
- **E1~E10·N1~N11·§4 보안 전부 HANDLED**(파일:라인 근거) — BLOCKER/HIGH 0. N1(원 BLOCKER)은 원장 held-check로 실제 닫힘 확인.
- **신규 MEDIUM 1건 발견·수정(risk#1):** 아침 pre-pass가 합성 진입으로 opener 슬롯(entry_date=today)을 §2
  전에 원장에 넣어, opener 전략의 매도조건이 진입 바에 참이면 §2가 방금 넷팅한 포지션을 실제 wash 매도할 수
  있었다(비넷팅 경로와 divergence). → `netted_opener_sids`를 §2가 skip하도록 근본수정(재현 테스트 추가).
- **LOW 잔여(수용/후속):** ①아침 무겹침 시 dry-run capture 중복 브로커 조회(지연만·무상태변경) ②선물
  commission_saved는 명목기반 근사(표시 전용·§7대로 보수적 하한) ③잔여 청산 *거부* 시 유효여력 과대 —
  §5.5가 수용한 근사(브로커 거부=2차 안전망), 모의계좌 검증 대상.
- **판정:** 모의계좌 라이브 검증 후 머지 안전. 코드 레벨 정합·no-op 보존·보안 모두 확인.

**전 구현 검증:** 로컬 728 테스트 green(넷팅 단위 12+시나리오 18+gui_format 6 신규 포함). 골든 아침 경로 byte-identical 보존.

### 12.5 계좌 수준 모호성 — commingled 부분 수동매도 (넷팅 외재·정직·E11)

발견 경위: "연동 전 기존 포지션·수동 조작 대응" 분석 중 `reconcile_ledger` 코드 검토로 확인. **넷팅이
만들거나 악화시키는 결함이 아니라, 단일 계좌에 두 출처(전략 개설분 + 기존/수동 보유)가 섞였을 때의
본질적 모호성**이라 여기 기록한다(넷팅이 이를 상속만 함).

- **기존/수동 매수분은 원장 미편입·불건드림.** KIS 실잔고엔 있으나 원장에 없으면 `external_extras`로
  분류돼 자동매매가 손대지 않는다([analytics.py:511-520](../../local/localapp/analytics.py#L511)). 넷팅·청산·진입
  순회는 전부 원장(=앱 개설분)만 대상([trader.py:1615](../../local/localapp/trader.py#L1615)·1667·1920) →
  기존/수동 포지션이 상쇄·청산 대상이 될 경로 자체가 없다. (유일 예외 = 비상 전량청산 `liquidate_all_held`,
  안전기능이라 실보유 직접 청산 — 사용자 명시 킬스위치 시에만.)
- **모호성의 실체:** 전략과 수동보유가 **같은 종목·같은 side**로 섞이면 reconcile는 `(symbol, side)`
  **합계**만 본다([analytics.py:474-487](../../local/localapp/analytics.py#L474)). 사용자가 그 종목을 **일부만**
  수동매도하면 "전략 몫이 팔렸는지 수동 몫이 팔렸는지" 정보가 없어 구분 불가:
  - 실보유 ≥ 원장합계 → 전략 원장 유지(사용자가 자기 초과분만 판 것으로 간주).
  - 실보유 < 원장합계 → 부족분을 전략 몫에서 차감(orphan 처리·[trader.py:334-367](../../local/localapp/trader.py#L334)).
- **왜 넷팅과 무관:** 넷팅은 원장 파생 의도만 `(contract_code, 포지션 side)`로 상쇄하므로 수동/기존 분을
  **만들지도 상쇄하지도 않는다**. §13.3의 "enrich 종목당 1전략"·"롱숏 동시 보유"와 동류의 **선재 한계**다.
- **영향 경계(자금 안전 무영향):** 청산은 항상 `min(실보유, 원장)` 클램프([trader.py:216 `clamp_sell_qty`](../../local/localapp/trader.py#L216))라
  초과청산·유령청산은 발생하지 않는다. 최악은 원장 수량 **귀속 오차**뿐이며, 정보 부족으로 인한 것이라 코드로
  완전 해소 불가. EOD reconcile(15:50)가 실잔고에 재수렴시킨다.
- **대응:** 사용자 안내로 "전략 연동 종목은 수동 혼합매매를 가급적 지양" 권고. 필요 시 전략 종목 보호 경고
  (웹/로컬 표시)를 **별도 트랙** 후속으로(넷팅 스코프 밖).

## 13. 근본 아키텍처 v2 — "합성 체결 기반 포지션 핸드오프" (감사 반영, §3~§5.4 메커니즘 대체)

**핵심 원리:** 넷팅 = **(contract_code, 포지션 side) 단위의 내부 포지션 핸드오프**를, 별도 "무체결 우주"가 아니라
**기존 `_apply_fill` + intent 저널 + `_CYCLE_LOCK` 기계를 그대로 태운 두 건의 합성 체결(ref_price·수수료 0)**로
실현한다. 이 한 가지 선택이 N1·N2·N5·N8·N10·N11을 *클래스로* 해소한다(각 기계가 이미 올바르게 처리).

### 13.1 3단계 파이프라인 (단일 스냅샷)
1. **PLAN** (브로커 write 없음): 창 시작 1회 스냅샷. 청산 의도(실보유 **클램프**·`contract_code`·포지션 side)와
   진입 의도를 산출. 진입 사이징은 **로컬 여력원장**으로 — 스냅샷 시드 + 같은 contract 청산이 회수할 여력 크레딧
   (클램프된 수량만·N7) + 진입마다 차감(N2·N9). 게이트(킬스위치·drawdown)는 이 단계에서 적용 → 막힌 진입은 의도 없음(E5).
   **창 전체를 로컬 여력원장 하나로** 사이징(live 재조회 분기 제거 → N9 해소·no-op은 골든으로 검증).
2. **NET** (순수 함수): **(contract_code, 포지션 side)별로만** 상쇄 — 그 side의 **open↔close**만 핸드오프
   (롱진입↔롱청산 ✅ / 숏진입↔숏청산 ✅). 핸드오프량 = min(그 side의 여는 수량, 닫는 수량).
   **교차 side는 절대 상쇄 안 함**(롱↔숏, 진입이든 청산이든 — N10 + §13.4). 잔여 = 실주문. 배분 strategy_id FIFO(E10).
3. **APPLY** (단일 락 임계구역·원자 저장·WS reload 억제 → N6):
   - 핸드오프 1단위 = **두 건의 합성 체결**을 `_apply_fill`로: 닫는 전략 exit(ref_price 실현·통화/승수 기존 로직 → N8),
     여는 전략 entry(ref_price 개시·peak=ref는 여는 전략엔 정상 → N4 트레일링 해소). `netted=true`·order_no=합성·수수료 0.
   - **intent 저널 시드 기록**(닫힘·열림) → 재시도/catch-up 멱등(N1 BLOCKER 해소).
   - **슬리피지 skip**: `record_slippage`가 `netted` leg 제외(N2).
   - 잔여(net_S)는 **기존 `_submit_*` 실주문**(실체결·기존 안전망 전부). 핸드오프는 (contract,side) 총량 보존 →
     reconcile은 잔여 체결까지 정합(N5 해소).
4. **REPORT:** cycle 요약에 `n_netted`·`commission_saved_krw`를 **실거래수와 분리** 카운트(N3). 타임라인 false-green 방지.

### 13.2 이 아키텍처가 감사를 닫는 방식 (클래스 단위)
| 뿌리/케이스 | 닫는 방식 |
|---|---|
| (가) 무체결 우주 · N1·N2·N5·N8·N11 | 합성 체결이 `_apply_fill`+intent+락을 **재사용** → 기계가 이미 올바름 |
| (나) side 불일치 · N10·N4 | (contract, **포지션 side**)로만 상쇄 · 여는 전략에 fresh peak는 정상 |
| E1·E2·N7·N9 | PLAN의 **로컬 여력원장**(클램프 크레딧·순차 차감·창 단일 자금원) |
| E6 | 넷팅 키 = **contract_code**(물리 계약) |
| N6 | APPLY 전체가 단일 락 임계구역 |
| N3 | netted 별도 카운트 |

### 13.3 잔여 위험 (정직)
- **no-op 골든 보존:** 로컬 여력원장 사이징이 현행 live-재조회와 같은 수량을 내는지 — **골든/시나리오 테스트로 실증**(추정 금지).
- **유효여력 근사:** 브로커 마진 비선형이면 근사 → 모의계좌 라이브 검증 필요(설계단계 검증 불가 명시).
- **enrich 종목당 1전략:** 다전략-1종목 표시 붕괴는 **넷팅 이전부터의 선재 한계**(A·B 동시 연동 시 이미 발생). 넷팅과 별개 트랙으로 표면화·후속 수정.
- **한 종목 롱·숏 동시 보유(다계좌 아님):** 단일 선물계좌는 같은 contract를 net한다(상계) → 롱+숏 동시 로컬 원장은 선재 미지원 영역. 넷팅은 이를 **만들지 않으며**(교차 side 미상쇄), 기존 동작 보존.

### 13.4 왜 교차 side(롱↔숏)는 넷팅하지 않는가 (설계 확정)

**넷팅의 정당성 = "물리적 포지션이 지속되고 소유 전략만 바뀐다".** 같은 side의 open↔close만 이 조건을 만족한다.

- **같은 side (롱진입+롱청산 / 숏진입+숏청산):** 실물 포지션이 그대로 남고 소유만 X→Y. 손익 = 그 실물의
  진짜 돈을 기준가에서 분할 → 정직. **넷팅함.**
- **교차 side (롱+숏 동시 진입 / 롱+숏 동시 청산):** 지속 실물 포지션이 **없음**. 국내 선물계좌는 같은 계약을
  **net 보유**(롱5·숏5 동시 불가·상계 flat) → 두 다리는 정반대 베팅. 넷팅해 장부 포지션을 만들면 계좌는 flat인데
  장부는 각자 손익을 주장 → **실재하지 않는 손익(허구)**. (예: X롱 @100→@110 +50, Y숏 @100→@105 −25,
  합 +25인데 계좌 실손익 0.) 같은-side 핸드오프는 실물이 내내 존재해 합이 진짜 손익 — 이 차이가 가부를 가른다.
  **넷팅 안 함.**

**추가 — 교차 side 설정은 선재 병리:** X롱+Y숏(같은 계약·계좌)은 보장된 wash+이중수수료라 무의미하고,
브로커 net vs 원장 gross로 reconcile를 깨뜨린다(넷팅 무관). 교차 side를 **넷팅하지 않으면** 실주문이 나가
reconcile가 이 병리를 **표면화**한다(넷팅하면 무체결로 은폐). 따라서 미넷팅이 안전망 관점에서도 옳다.
넷팅은 이 병리를 만들지도 숨기지도 않는다.
