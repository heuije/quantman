# Quant platform API server.
# Also install the core package: pip install -e ../core
fastapi>=0.136
uvicorn[standard]>=0.47
sqlmodel>=0.0.38
PyJWT>=2.10
bcrypt>=4.1
email-validator>=2.1
httpx>=0.27
psycopg[binary]>=3.2          # PostgreSQL driver (production DB)
google-auth[requests]>=2.30   # Google social login - ID token verification
apscheduler>=3.10             # Daily KIS symbol-master refresh cron
requests>=2.31                # KIS .mst download (also pulled transitively; explicit)
finance-datareader>=0.9       # NAVER-backed Korean symbol data (KRX site blocks scraping)
pandas>=2.0                   # fdr returns pandas DataFrame
python-multipart>=0.0.18      # FastAPI file upload (multipart/form-data)
exchange_calendars>=4.5       # Q2+Q8 - daily KR/US session refresh (incl. ad-hoc holidays)
