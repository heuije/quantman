# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# 가상환경
.venv/
venv/

# 비밀 · 런타임 데이터 (절대 커밋 금지)
.env
.env.*
!.env.example
*.db
.token_cache.json
.kis_token.json
ledger.json
equity.json
trades.jsonl
pending_snapshot.json
orders.jsonl
cycles.jsonl
killswitch.json
slippage.json
pending_orders.json
.kis_master_pushed.txt

# 빌드 산출물
dist/
dist2/
dist_rel/
build/
build2/
build_rel/
Output/
*.zip

# 자동 갱신 데이터 캐시 — 서버 cron이 fetch하므로 git 저장 불필요.
# ticker_db.json·user_stocks.json은 작은 metadata라 tracked 유지.
core/data/*.parquet
core/data/fundamentals/*.parquet
core/data/managed_kr_stocks.json
core/data/managed_overseas_stocks.json
core/data/us_market_caps.json
core/data/_classification.json
core/data/_listing.json
core/data/_generation

# 개별기업 분석(HOME) 생성 캐시 — 서버 read-through + cron이 재생성하므로 git 저장 불필요
server/app/data/company_profiles.json
server/app/data/earnings/
server/app/data/financials/

# 대용량 데이터셋 번들(로컬앱 배포용 생성물, 155MB+) · 외부 doc 캐시 — repo 미포함
core/dataset-bundle.tar.zst
core/dataset-bundle.meta
docs_cache/

# 웹
node_modules/
# 이 repo는 package-lock.json(npm)을 추적 — bun install이 만드는 bun.lock은 파생물이라 미추적.
web/bun.lock

# 로그
*.log
*.log.err
logs/

# OS
.DS_Store
Thumbs.db

.vercel
.gstack/
_apis_meta.json
