# Quant platform local app.
# Also install the core package: pip install -e ../core
keyring>=25.0
APScheduler>=3.10
requests>=2.31
# For packaging (Stage 2-3 release):
# pystray>=0.19   Pillow>=10.0   pyinstaller>=6.0
